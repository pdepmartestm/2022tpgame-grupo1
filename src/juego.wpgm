import wollok.game.*
import characters.*
import elements.*
import Levels.*

program juego {
	
	//PROPIEDADES DEL JUEGO
	const ancho = 15
	const alto = 15
	game.cellSize(60)
	game.height(alto)
	game.width(ancho)
	game.title("Laberinto")
	
	//GAME 
	level3.start()
	

	game.start()
}
